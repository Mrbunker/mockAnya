export enum Kind {
  image = "image",
  text = "text",
  video = "video",
  audio = "audio",
}

export enum View {
  image = "image",
  text = "text",
  video = "video",
  audio = "audio",
  settings = "settings",
}
